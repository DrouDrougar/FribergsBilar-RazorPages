@{ //3 different layouts based on which cookie is active
    if (ViewData["UserCookies"] == null && ViewData["AdminCookies"] == null)
    {
        Layout = "_Layout";
    }
    if (/* ViewData["UserCookies"] == null &&  */ViewData["AdminCookies"] != null)
    {
        Layout = "_LayoutAdmin";
    }
        if (ViewData["UserCookies"] != null/*  && ViewData["AdminCookies"] == null */)
    {
        Layout = "_LayoutUser";
    }
}
